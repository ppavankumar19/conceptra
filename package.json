{
  "name": "conceptra",
  "version": "1.0.0",
  "scripts": {
    "build": "git clone https://github.com/flutter/flutter.git && cd flutter && git checkout 3.19.3 && cd .. && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter config --no-analytics && cd frontend && flutter clean && flutter pub get && flutter build web --release --dart-define=API_BASE_URL=https://conceptra-api.onrender.com/api/v1"
  },
  "dependencies": {}
}
