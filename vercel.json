{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b 3.19.3 /tmp/flutter && export PATH=\"$PATH:/tmp/flutter/bin\" && flutter config --no-analytics && git config --global --add safe.directory '*' && cd frontend && flutter build web --release --dart-define=API_BASE_URL=https://conceptra-api.onrender.com/api/v1",
  "outputDirectory": "frontend/build/web",
  "installCommand": "echo 'Skipping npm install'"
}
